import mapboxgl from 'mapbox-gl';
// import MapboxGeocoder from '@mapbox/mapbox-gl-geocoder';

mapboxgl.accessToken = MAPBOX_ACCESS_TOKEN;

const key = {};

export { mapboxgl /*, MapboxGeocoder*/, key };
