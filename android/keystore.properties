# Release signing credentials for the Android app.
#
# Copy this file to `android/keystore.properties` (which is gitignored) and fill in
# the values from the keystore you created in Android Studio's
# "Generate Signed Bundle / APK" wizard. NEVER commit the real file or the keystore.
#
# Paths are resolved relative to the `android/` directory (rootProject), or you can
# use an absolute path. CI can supply these via env vars instead:
#   WTMG_KEYSTORE_FILE, WTMG_KEYSTORE_PASSWORD, WTMG_KEY_ALIAS, WTMG_KEY_PASSWORD

storeFile=/Users/thor/Work/Slowby/mobile app/google dev account/keystore.jks
storePassword=Acid-Coronary6-Celibacy
keyAlias=key0
keyPassword=Acid-Coronary6-Celibacy
